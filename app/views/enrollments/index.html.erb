<h2>Enrollments</h2>

<table>
  <thead>
    <tr>
      <th>Course</th>
      <th>Teacher</th>
    </tr>
  </thead>
  <tbody>
    <%= render @dashboard.enrollments %>
  </tbody>
</table>

<%= link_to 'New enrollment', new_enrollment_path if teacher_signed_in? %>
