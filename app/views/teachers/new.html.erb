<%= form_for @form.teacher do |f| %>
  <%= render 'shared/error_messages', resource: @form.teacher if @form.errors.any? %>

  <%= f.label :email %>
  <%= f.email_field :email %>

  <%= button_tag 'Save', type: 'submit' %>
<% end %>

<%= link_to 'Back to Teachers', teachers_path %>
